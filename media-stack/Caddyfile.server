# Прод: портал + три сервиса на своих FQDN; автоматический HTTPS (Let's Encrypt) на каждом имени.
# В .env задайте PUBLIC_HOSTNAME, PROWLARR_HOSTNAME, TRANSMISSION_HOSTNAME, JELLYFIN_HOSTNAME (и DNS A/AAAA на сервер).
# Локально по-прежнему Caddyfile + snippets/site.caddy (пути /prowlarr и т.д.).

import snippets/site-subdomains.caddy
